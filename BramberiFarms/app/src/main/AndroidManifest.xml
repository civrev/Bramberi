<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobileapps.bramberifarms">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SpinnerTestActivity" />
        <activity android:name=".InsertMenuActivity" />
        <activity android:name=".InsertBerryActivity" />
        <activity android:name=".InsertStatActivity2" />
        <activity android:name=".DeletionActivity" />
        <activity android:name=".CompareStatsActivity" />
        <activity android:name=".WeatherActivity"></activity>
    </application>

</manifest>